/**
 * Interface defining a Character with an attack method.
 */
export interface Character {
  /**
   * Method representing the character's attack behavior.
   * @returns A string describing the character's attack.
   */
  attack(): string;
}
